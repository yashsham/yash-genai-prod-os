# Infrastructure Directory

This directory contains infrastructure configuration and options.

## Structure
- `docker/`: Docker related files (Dockerfiles, compose files, etc.)
- `terraform/`: Terraform configuration files for IaC.
- `ci-cd.yaml`: CI/CD pipeline configuration.
